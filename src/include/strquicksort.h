#ifndef STRQUICKSORT_H
#define STRQUICKSORT_H

extern struct key_words * StringQuickSort(struct key_words *R, int l);
extern struct key_words * NewArray(int size);
extern void add(struct key_words *R, char *S);

#endif
